import { ChartConfig } from "@/components/ui/chart";

const categoryChart = {
    n_reports: {
        label: "No. Reportes",
        color: "#60a5fa",
    },
} satisfies ChartConfig;

export default categoryChart;
